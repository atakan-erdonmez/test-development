
 ##2
 sudo apt install watchdog
sudo nano /boot/firmware/config.txt
add>>
dtparam=watchdog=on


then;
sudo nano /etc/watchdog.conf

watchdog-device = /dev/watchdog

What it does: Tells the software where the physical hardware chip is located.

/

watchdog-timeout = 15

What it does: The hardware will wait 15 seconds for a "kick" before resetting.

realtime = yes

What it does: Ensures the watchdog process doesn't get "paused" by other high-priority tasks.




